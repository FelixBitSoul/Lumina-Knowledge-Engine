# Production environment configuration

# API settings
api:
  host: "0.0.0.0"
  port: 8000

# CORS settings
cors:
  origins:
    - "https://portal.lumina.dev"
    - "https://www.lumina.dev"

# Model settings
model:
  name: "all-MiniLM-L6-v2"
  cache_dir: "/app/models"

# Qdrant settings
qdrant:
  host: "qdrant"
  port: 6333
  collection: "knowledge_base"

# Log settings
log:
  level: "info"
