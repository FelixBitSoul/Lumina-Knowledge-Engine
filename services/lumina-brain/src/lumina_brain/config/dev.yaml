# Development environment configuration

# API settings
api:
  host: "0.0.0.0"
  port: 8000

# CORS settings
cors:
  origins:
    - "http://localhost:3000"
    - "http://127.0.0.1:3000"
    - "http://localhost:8080"

# Model settings
model:
  name: "all-MiniLM-L6-v2"
  cache_dir: "./models"

# Qdrant settings
qdrant:
  host: "localhost"
  port: 6333
  collection: "knowledge_base_dev"

# Log settings
log:
  level: "debug"
